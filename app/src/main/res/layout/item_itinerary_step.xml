<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Transport separator (shown between stops) -->
    <LinearLayout
        android:id="@+id/travel_separator"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="32dp"
        android:paddingVertical="6dp"
        android:visibility="gone">

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1"
            android:background="@color/app_divider" />

        <TextView
            android:id="@+id/text_travel_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="10dp"
            android:text="🚶 15 min"
            android:textColor="@color/app_text_secondary"
            android:textSize="11sp" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1"
            android:background="@color/app_divider" />
    </LinearLayout>

<androidx.cardview.widget.CardView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="24dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="@color/app_card">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Top Header: Time, Slot Label, Actions -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Time Indicator & Slot Label group -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <!-- Clock Icon -->
                <ImageView
                    android:layout_width="14dp"
                    android:layout_height="14dp"
                    android:src="@drawable/ic_clock"
                    app:tint="@color/primary" />

                <!-- Time text -->
                <TextView
                    android:id="@+id/text_step_time"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="6dp"
                    android:text="09:00 - 10:00"
                    android:textColor="@color/primary"
                    android:textSize="13sp"
                    android:fontFamily="sans-serif-black" />

                <!-- Divider dot -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:layout_marginEnd="8dp"
                    android:text="•"
                    android:textColor="@color/app_text_secondary"
                    android:textSize="13sp" />

                <!-- Slot label -->
                <TextView
                    android:id="@+id/text_slot_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="MIC DEJUN"
                    android:textColor="@color/app_text_secondary"
                    android:textSize="10sp"
                    android:textAllCaps="true"
                    android:letterSpacing="0.05"
                    android:fontFamily="sans-serif-medium" />
            </LinearLayout>

            <!-- Action buttons -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageButton
                    android:id="@+id/btn_step_swap"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:padding="6dp"
                    android:src="@drawable/ic_refresh"
                    app:tint="@color/primary" />

                <ImageButton
                    android:id="@+id/btn_step_delete"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:layout_marginStart="8dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:padding="6dp"
                    android:src="@drawable/ic_delete"
                    app:tint="@color/error" />
            </LinearLayout>
        </LinearLayout>

        <!-- Subtle Divider Line -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/app_divider"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="12dp" />

        <!-- Navigare Google Maps -->
        <Button
            android:id="@+id/btn_navigate"
            android:layout_width="match_parent"
            android:layout_height="36dp"
            android:layout_marginBottom="10dp"
            android:text="Navighează cu Google Maps →"
            android:textSize="11sp"
            android:textColor="@color/white"
            android:backgroundTint="@color/primary"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:visibility="gone" />

        <!-- Tip / Recomandare tranzitie -->
        <TextView
            android:id="@+id/text_step_tip"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="10dp"
            android:paddingHorizontal="10dp"
            android:paddingVertical="6dp"
            android:background="@drawable/bg_glass_card"
            android:drawablePadding="6dp"
            android:text="Prima oprire a zilei!"
            android:textColor="@color/primary"
            android:textSize="11sp"
            android:textStyle="italic"
            android:visibility="gone" />

        <!-- Bottom row: Image & Location details -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Location Image with rounded corners -->
            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/img_step"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:scaleType="centerCrop"
                app:shapeAppearanceOverlay="@style/RoundedSquare"
                android:background="@drawable/bg_glass_card"
                android:padding="2dp"
                android:src="@drawable/placeholder_place" />

            <!-- Details container (Name, Address, Cost, Warning, Distance) -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="14dp"
                android:orientation="vertical">

                <!-- Location Name -->
                <TextView
                    android:id="@+id/text_step_name"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Nume Locație"
                    android:textColor="@color/app_text_primary"
                    android:textSize="15sp"
                    android:fontFamily="sans-serif-medium"
                    android:maxLines="2"
                    android:ellipsize="end" />

                <!-- Location Address -->
                <TextView
                    android:id="@+id/text_step_address"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:text="Adresă locație"
                    android:textColor="@color/app_text_secondary"
                    android:textSize="11sp"
                    android:maxLines="2"
                    android:ellipsize="end" />

                <!-- Badges row (Cost, Distance, Warning) -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <!-- Estimated Cost -->
                    <TextView
                        android:id="@+id/text_step_cost"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:background="@drawable/bg_glass_card"
                        android:paddingHorizontal="8dp"
                        android:paddingVertical="2dp"
                        android:text="~50 RON"
                        android:textColor="@color/gold_accent"
                        android:textSize="10sp"
                        android:textStyle="bold" />

                    <!-- Distance -->
                    <TextView
                        android:id="@+id/text_distance"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:text="1.2 km"
                        android:textColor="@color/primary"
                        android:textSize="10sp" />

                    <!-- Warning Closed Now -->
                    <TextView
                        android:id="@+id/badge_closed_warning"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:background="@drawable/bg_pill_primary_faded"
                        android:backgroundTint="@color/error"
                        android:paddingHorizontal="8dp"
                        android:paddingVertical="2dp"
                        android:text="ÎNCHIS ACUM"
                        android:textColor="@color/white"
                        android:textSize="9sp"
                        android:textStyle="bold"
                        android:visibility="gone" />
                </LinearLayout>
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>

</LinearLayout>

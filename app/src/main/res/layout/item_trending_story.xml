<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="80dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="4dp"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackground">

    <!-- Story Ring Container -->
    <FrameLayout
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginBottom="6dp">

        <!-- Outer Glowing Ring -->
        <View
            android:id="@+id/story_ring"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_story_ring_gradient" />

        <!-- Inner Content Circle (Glassmorphic) -->
        <FrameLayout
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="center"
            android:background="@drawable/bg_story_inner_circle">

            <!-- Icon or Emoji -->
            <TextView
                android:id="@+id/story_emoji"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:textSize="22sp"
                android:text="☕" />
        </FrameLayout>

        <!-- Dynamic Indicator / Flame Badge -->
        <FrameLayout
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="bottom|end"
            android:background="@drawable/circle_red"
            android:backgroundTint="@color/error"
            android:elevation="4dp">

            <TextView
                android:id="@+id/story_hype_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="🔥"
                android:textSize="11sp" />
        </FrameLayout>

    </FrameLayout>

    <!-- Name Label -->
    <TextView
        android:id="@+id/story_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="Locație"
        android:textColor="@color/app_text_primary"
        android:textSize="11sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif-medium" />

    <!-- Hype Percent (Subtitle) -->
    <TextView
        android:id="@+id/story_hype_score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="95%"
        android:textColor="@color/primary"
        android:textSize="9sp"
        android:textStyle="bold"
        android:fontFamily="sans-serif-condensed" />

</LinearLayout>
